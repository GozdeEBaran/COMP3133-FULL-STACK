// Exercise 1

let firstName: string = "Gozde";
let lastName: string = "Baran";

const greet = (first: string, last: string): void => {
  console.log(`Hello ${first} ${last}`);
};

greet(firstName, lastName);
